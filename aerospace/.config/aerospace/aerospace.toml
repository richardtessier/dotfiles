# Place a copy of this config to ~/.aerospace.toml
# After that, you can edit ~/.aerospace.toml to your liking

# You can use it to add commands that run after login to macOS user session.
# 'start-at-login' needs to be 'true' for 'after-login-command' to work
# Available commands: https://nikitabobko.github.io/AeroSpace/commands
after-login-command = []

# You can use it to add commands that run after AeroSpace startup.
# 'after-startup-command' is run after 'after-login-command'
# Available commands : https://nikitabobko.github.io/AeroSpace/commands
after-startup-command = []

# Start AeroSpace at login
start-at-login = true

# Normalizations. See: https://nikitabobko.github.io/AeroSpace/guide#normalization
enable-normalization-flatten-containers = true
enable-normalization-opposite-orientation-for-nested-containers = true

# See: https://nikitabobko.github.io/AeroSpace/guide#layouts
# The 'accordion-padding' specifies the size of accordion padding
# You can set 0 to disable the padding feature
accordion-padding = 100

# Possible values: tiles|accordion
default-root-container-layout = 'tiles'

# Possible values: horizontal|vertical|auto
# 'auto' means: wide monitor (anything wider than high) gets horizontal orientation,
#               tall monitor (anything higher than wide) gets vertical orientation
default-root-container-orientation = 'auto'

# Mouse follows focus when focused monitor changes
# Drop it from your config, if you don't like this behavior
# See https://nikitabobko.github.io/AeroSpace/guide#on-focus-changed-callbacks
# See https://nikitabobko.github.io/AeroSpace/commands#move-mouse
# Fallback value (if you omit the key): on-focused-monitor-changed = []
on-focused-monitor-changed = ['move-mouse monitor-lazy-center']

# You can effectively turn off macOS "Hide application" (cmd-h) feature by toggling this flag
# Useful if you don't use this macOS feature, but accidentally hit cmd-h or cmd-alt-h key
# Also see: https://nikitabobko.github.io/AeroSpace/goodies#disable-hide-app
automatically-unhide-macos-hidden-apps = false

# Possible values: (qwerty|dvorak)
# See https://nikitabobko.github.io/AeroSpace/guide#key-mapping
[key-mapping]
preset = 'qwerty'

# Gaps between windows (inner-*) and between monitor edges (outer-*).
# Possible values:
# - Constant:     gaps.outer.top = 8
# - Per monitor:  gaps.outer.top = [{ monitor.main = 16 }, { monitor."some-pattern" = 32 }, 24]
#                 In this example, 24 is a default value when there is no match.
#                 Monitor pattern is the same as for 'workspace-to-monitor-force-assignment'.
#                 See: https://nikitabobko.github.io/AeroSpace/guide#assign-workspaces-to-monitors
[gaps]
inner.horizontal = 10
inner.vertical =   10
outer.left =       0
outer.bottom =     [{monitor."Built-in Display" = 0}, 
                    {monitor."DELL S3220DGF" = 0}, 
                    {monitor."DELL U2720Q" = 0},
                    {monitor."DELL U2718Q" = 0},
                    {monitor."LG ULTRAGEAR" = 0},
                    {monitor."LG Ultra HD" = 200}, 
                    {monitor."PHL BDM4350" = 125}, 
                    0]
outer.top =        0
outer.right =      0

[workspace-to-monitor-force-assignment]
1 = 'main'                       # Main monitor
2 = 'main'                       # Main monitor
3 = 'main'                       # Main monitor
4 = '^built-in.*display$'        # Case insensitive regex match
5 = '^built-in.*display$'        # Case insensitive regex match
6 = '^built-in.*display$'        # Case insensitive regex match
Z = '^built-in.*display$'        # Case insensitive regex match

# My custom config section (richardtessier)
[key-mapping.key-notation-to-key-code]
# unicorn = 'ù' # Would be nice to be able to use CSA keys for mappings

# Apps to worksapce assignment
[[on-window-detected]]
if.app-name-regex-substring = '^outlook \(pwa\)$'
run = "move-node-to-workspace 1"

[[on-window-detected]]
if.app-id = 'com.tinyspeck.slackmacgap'
run = "move-node-to-workspace 1"

[[on-window-detected]]
if.app-id = 'com.brave.Browser'
run = "move-node-to-workspace B"

[[on-window-detected]]
if.app-id = 'com.google.Chrome'
run = "move-node-to-workspace C"

[[on-window-detected]]
if.app-id = 'com.microsoft.edgemac'
run = "move-node-to-workspace 1"
# run = ["layout floating", "move-node-to-workspace E" ]

[[on-window-detected]]
if.app-id = 'com.openai.chat'
run = ["move-node-to-workspace G"]

[[on-window-detected]]
if.app-id = 'ai.perplexity.mac'
run = ["move-node-to-workspace G" ]

[[on-window-detected]]
if.app-id = 'com.microsoft.PowerPoint'
run = ["move-node-to-workspace P" ]

[[on-window-detected]]
if.app-id = 'com.googlecode.iterm2'
run = ["layout floating", "move-node-to-workspace T" ]

[[on-window-detected]]
if.app-id = 'com.microsoft.teams2'
run = ["layout floating", "move-node-to-workspace Z" ]

[[on-window-detected]]
if.app-id = 'us.zoom.xos'
run = "move-node-to-workspace Z"
check-further-callbacks = true

# [[on-window-detected]]
# if.window-title-regex-substring = 'zoom meeting$'
# run = "move-node-to-workspace Z"

[[on-window-detected]]
if.window-title-regex-substring = '^zoom workplace$'
run = "layout floating"

# Apple apps floating
[[on-window-detected]]
if.app-name-regex-substring = '^calendar$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^contacts$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^facetime$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^finder$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^messages$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^passwords$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^preview$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'quicktime'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^safari$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^stocks$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^tips$'
run = 'layout floating'

# Other apps floating
[[on-window-detected]]
if.app-name-regex-substring = '1password$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^alfred preferences$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = 'battle'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^hey$'
run = 'layout floating'

[[on-window-detected]]
if.app-name-regex-substring = '^spotify$'
run = 'layout floating'

# All apps workspace

# 'main' binding mode declaration
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
# 'main' binding mode must be always presented
# Fallback value (if you omit the key): mode.main.binding = {}
[mode.main.binding]

# All possible keys:
# - Letters.        a, b, c, ..., z
# - Numbers.        0, 1, 2, ..., 9
# - Keypad numbers. keypad0, keypad1, keypad2, ..., keypad9
# - F-keys.         f1, f2, ..., f20
# - Special keys.   minus, equal, period, comma, slash, backslash, quote, semicolon, backtick,
#                   leftSquareBracket, rightSquareBracket, space, enter, esc, backspace, tab
# - Keypad special. keypadClear, keypadDecimalMark, keypadDivide, keypadEnter, keypadEqual,
#                   keypadMinus, keypadMultiply, keypadPlus
# - Arrows.         left, down, up, right

# All possible modifiers: cmd, alt, ctrl, shift

# All possible commands: https://nikitabobko.github.io/AeroSpace/commands

# See: https://nikitabobko.github.io/AeroSpace/commands#exec-and-forget
# You can uncomment the following lines to open up terminal with alt + enter shortcut (like in i3)
# alt-enter = '''exec-and-forget osascript -e '
# tell application "Terminal"
#     do script
#     activate
# end tell'
# '''

# My custom commands
alt-f = 'fullscreen'
alt-n = 'move-node-to-monitor --wrap-around --focus-follows-window prev'
alt-m = 'move-node-to-monitor --wrap-around --focus-follows-window next'
alt-shift-n = 'move-node-to-workspace --wrap-around --focus-follows-window prev'
alt-shift-m = 'move-node-to-workspace --wrap-around --focus-follows-window next'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
# alt-slash = 'layout tiles horizontal vertical'
# alt-comma = 'layout accordion horizontal vertical'

# See: https://nikitabobko.github.io/AeroSpace/commands#focus
alt-h = 'focus left'
alt-j = 'focus down'
alt-k = 'focus up'
alt-l = 'focus right'
alt-q = 'layout floating tiling'

# See: https://nikitabobko.github.io/AeroSpace/commands#move
alt-shift-h = 'move left'
alt-shift-j = 'move down'
alt-shift-k = 'move up'
alt-shift-l = 'move right'

# See: https://nikitabobko.github.io/AeroSpace/commands#layout
alt-shift-q = 'layout h_accordion h_tiles'
alt-ctrl-shift-q = 'exec-and-forget bash -c "$HOME/dotfiles/scripts/aerospace_layout_all.sh"'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
alt-shift-minus = 'resize smart -50'
alt-shift-equal = 'resize smart +50'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace
alt-1 = 'workspace 1 --auto-back-and-forth'
alt-2 = 'workspace 2 --auto-back-and-forth'
alt-3 = 'workspace 3 --auto-back-and-forth'
alt-4 = 'workspace 4 --auto-back-and-forth'
alt-5 = 'workspace 5 --auto-back-and-forth'
alt-6 = 'workspace 6 --auto-back-and-forth'
# alt-7 = 'workspace 7'
# alt-8 = 'workspace 8'
# alt-9 = 'workspace 9'
# alt-a = 'workspace A' # In your config, you can drop workspace bindings that you don't need
alt-b = 'workspace B --auto-back-and-forth'
alt-c = 'workspace C --auto-back-and-forth'
# alt-d = 'workspace D'
alt-e = 'workspace E --auto-back-and-forth'
# alt-f = 'workspace F'
alt-g = 'workspace G --auto-back-and-forth'
# alt-i = 'workspace I'
# alt-m = 'workspace M'
# alt-n = 'workspace N'
# alt-o = 'workspace O'
alt-p = 'workspace P --auto-back-and-forth'
# alt-q = 'workspace Q'
# alt-r = 'workspace R'
# alt-s = 'workspace S'
alt-t = 'workspace T --auto-back-and-forth'
# alt-u = 'workspace U'
# alt-v = 'workspace V'
# alt-w = 'workspace W'
# alt-x = 'workspace X'
# alt-y = 'workspace Y'
alt-z = 'workspace Z --auto-back-and-forth'

# See: https://nikitabobko.github.io/AeroSpace/commands#move-node-to-workspace
alt-shift-1 = 'move-node-to-workspace --focus-follows-window 1'
alt-shift-2 = 'move-node-to-workspace --focus-follows-window 2'
alt-shift-3 = 'move-node-to-workspace --focus-follows-window 3'
alt-shift-4 = 'move-node-to-workspace --focus-follows-window 4'
alt-shift-5 = 'move-node-to-workspace --focus-follows-window 5'
alt-shift-6 = 'move-node-to-workspace --focus-follows-window 6'
# alt-shift-7 = 'move-node-to-workspace 7'
# alt-shift-8 = 'move-node-to-workspace 8'
# alt-shift-9 = 'move-node-to-workspace 9'
# alt-shift-9 = 'move-node-to-workspace 9'
# alt-shift-a = 'move-node-to-workspace A'
alt-shift-b = 'move-node-to-workspace --focus-follows-window B'
alt-shift-c = 'move-node-to-workspace --focus-follows-window C'
# alt-shift-d = 'move-node-to-workspace D'
alt-shift-e = 'move-node-to-workspace --focus-follows-window E'
# alt-shift-f = 'move-node-to-workspace F'
alt-shift-g = 'move-node-to-workspace --focus-follows-window G'
# alt-shift-i = 'move-node-to-workspace I'
# alt-shift-m = 'move-node-to-workspace M'
# alt-shift-n = 'move-node-to-workspace N'
# alt-shift-o = 'move-node-to-workspace O'
alt-shift-p = 'move-node-to-workspace P'
# alt-shift-q = 'move-node-to-workspace Q'
# alt-shift-r = 'move-node-to-workspace R'
# alt-shift-s = 'move-node-to-workspace S'
alt-shift-t = 'move-node-to-workspace --focus-follows-window T'
# alt-shift-u = 'move-node-to-workspace U'
# alt-shift-v = 'move-node-to-workspace V'
# alt-shift-w = 'move-node-to-workspace W'
# alt-shift-x = 'move-node-to-workspace X'
# alt-shift-y = 'move-node-to-workspace Y'
alt-shift-z = 'move-node-to-workspace --focus-follows-window Z'

# See: https://nikitabobko.github.io/AeroSpace/commands#workspace-back-and-forth
alt-tab = 'workspace-back-and-forth'
# See: https://nikitabobko.github.io/AeroSpace/commands#move-workspace-to-monitor
alt-shift-tab = 'move-workspace-to-monitor --wrap-around next'

# Navigate previous/next workspaces using arrow keys
ctrl-alt-right = 'workspace --wrap-around next'
ctrl-alt-left = 'workspace --wrap-around prev'

# See: https://nikitabobko.github.io/AeroSpace/commands#mode
alt-shift-semicolon = 'mode service'

# Custom modes (richardtessier)
alt-shift-x = 'mode rich'
# alt-shift-slash = 'mode rich'
# alt-shift-r = 'mode resize'
# [mode.resize.binding]

[mode.rich.binding]
h = 'resize width -50'
j = 'resize height +50'
k = 'resize height -50'
l = 'resize width +50'
b = 'balance-sizes'

# See: https://nikitabobko.github.io/AeroSpace/commands#resize
minus = 'resize smart -100'
equal = 'resize smart +100'

f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
r = ['flatten-workspace-tree', 'mode main'] # reset layout
# n = 'move-node-to-monitor --wrap-around --focus-follows-window next'
# p = 'move-node-to-monitor --wrap-around --focus-follows-window prev'
# shift-n = 'move-node-to-workspace --wrap-around --focus-follows-window next'
# shift-p = 'move-node-to-workspace --wrap-around --focus-follows-window prev'

tab = 'workspace --wrap-around next'
shift-tab = 'workspace --wrap-around prev'

period = 'layout tiles horizontal vertical'
comma = 'layout accordion horizontal vertical'

q = 'mode main'
enter = 'mode main'

c = ['reload-config', 'mode main']
esc = ['reload-config', 'mode main']

# 'service' binding mode declaration.
# See: https://nikitabobko.github.io/AeroSpace/guide#binding-modes
[mode.service.binding]
esc = ['reload-config', 'mode main']
r = ['flatten-workspace-tree', 'mode main'] # reset layout
f = ['layout floating tiling', 'mode main'] # Toggle between floating and tiling layout
backspace = ['close-all-windows-but-current', 'mode main']

# sticky is not yet supported https://github.com/nikitabobko/AeroSpace/issues/2
#s = ['layout sticky tiling', 'mode main']
alt-shift-h = ['join-with left', 'mode main']
alt-shift-j = ['join-with down', 'mode main']
alt-shift-k = ['join-with up', 'mode main']
alt-shift-l = ['join-with right', 'mode main']

down = 'volume down'
up = 'volume up'
shift-down = ['volume set 0', 'mode main']

# Rich's binding mode declaration.
# Define my fancy unicorn key notation
